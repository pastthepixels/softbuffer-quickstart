fn main() {
    /*
    // Example:
    let mut x = 0;
    let mut window = SoftbufferWindow::new(|window| {
        x += 1;
        println!("{}", x);
        vec![0xffffff; (window.width * window.height) as usize].into_boxed_slice()
    });
    window.run().expect("damn! :(");
    */
}
